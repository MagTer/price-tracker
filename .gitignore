
# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.mcp.json
.bg-shell/
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
.env
.env.*
!.env.example
!.env.template
node_modules/
.next/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
# Exception: the ICA night probe's run log is EVIDENCE, not runtime noise. 215 bytes
# recording that all five cron runs logged 4 rows each — which is what separates "one
# product 404'd at 03:00" from "that run partly failed". The blanket *.log rule silently
# dropped it from the commit that moved the experiment in (2026-09-06); `git add -A`
# said nothing.
!docs/experiments/ica-night-probe/cron.log
coverage/
.cache/
tmp/

# ── Price Tracker (Phase 1) ──
# Python
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Poetry
poetry.lock.bak

# Local env
*.db
.coverage

# Design handoffs — interactive prototypes delivered as a separate package, kept out of
# the repo on purpose (v0.47.0's "3a" handoff is likewise not here). They are a reference
# for one redesign round, not source; what survives review is documented in CLAUDE.md.
design_handoff_*/
