---
id: 260714-x48
slug: flodesanalys-atgarder-llm-berikning-sche
date: 2026-07-14
mode: quick
type: execute
autonomous: true
files_modified:
  - src/domain/parser.py
  - src/domain/extractors/jsonld.py
  - src/api/app.py
  - src/domain/service.py
  - src/domain/scheduler.py
  - src/api/admin.py
  - src/domain/notifier.py
  - src/mcp_server/server.py
  - src/api/templates/admin.html
  - pyproject.toml
  - tests/test_parser.py
  - tests/test_extractors.py
  - tests/test_service.py
  - tests/test_scheduler.py
  - tests/test_api.py
  - tests/test_mcp.py
  - tests/test_notifier.py
  - tests/test_email.py
must_haves:
  truths:
    - "ICA/Apotea/Med24/DOZ links (JSON-LD path) get package fields harvested by LLM on their first successful check and offer fields harvested when the effective price drops — reusing the already-fetched page, never an extra fetch, and never failing the check"
    - "A link whose check raises no longer retries every 5-minute cycle: next_check_at is advanced +1h on exception; a failed (non-exception) check on a weekday-scheduled link retries in 24h, not next week"
    - "Weekly summary shows the lowest kr/enhet across a watched product's links (latest point per link), with an absolute-price fallback labeled as such; the broken restart guard is gone"
    - "find_deals / get_current_deals shows only offers that are the LATEST point on their link and at most 7 days old — a superseded offer never appears"
    - "MCP list_products shows actual linked store names instead of 'Ej länkad' for every product, via one batched query"
    - "MCP check_price docstring says it returns the last OBSERVED price from history, no live fetch"
    - "Create-watch form no longer offers an absolute target-price input; existing watches still display theirs"
    - "Email templates use real Swedish diacritics (målpris, Jämförelsepris, prisöversikt, Här är, får, för, Lägsta)"
    - "App logs an error at startup when OPENROUTER_API_KEY is missing, without crashing"
    - "v0.4.0 tagged on a green, pushed main and the release build watched to success"
  artifacts:
    - "src/domain/parser.py: PriceParser.enrich_with_llm method + a shared private LLM-cascade helper"
    - "src/domain/service.py: module-level perform_price_check (the single fetch→extract→enrich→apply_scrape→record flow); PriceTrackerService.check_price DELETED"
    - "src/domain/extractors/jsonld.py: token-overlap name sanity check"
    - "src/domain/service.py: batched store-names-per-product helper for MCP list_products"
  key_links:
    - "scheduler._check_single_product AND admin.trigger_price_check both call service.perform_price_check — no third inline copy remains"
    - "perform_price_check queries the link's latest prior PricePoint by product_store_id (NEVER by (product_id, store_id) — AST gate) to decide the two enrichment triggers"
    - "enriched results keep raw_response source == 'jsonld' (+ enriched marker) so scheduler checks_jsonld stats stay truthful"
---

<objective>
Fix the eleven findings from the 2026-07-14 flow analysis of the live app (decisions LOCKED by Magnus in session): add LLM enrichment at two precise triggers so offer-based watches / find_deals / weekly deals and package autofill finally work for the four JSON-LD stores; fix the scheduler retry loop and weekday-failure rescheduling; fix the weekly summary (restart guard, lowest kr/enhet); fix the deals window; consolidate the three fetch→parse→record duplicates into one domain function; fix MCP list_products and the check_price docstring; phase out the absolute target-price input in the create-watch form; restore Swedish diacritics in emails; warn at startup when OPENROUTER_API_KEY is missing; add JSON-LD name sanity. Release as v0.4.0 (new capability = minor; NO schema change).

Output: all eleven fixes shipped with tests, suite green, v0.4.0 tagged and built.
</objective>

<context>
@./CLAUDE.md  (Swedish for user-visible text, English for code; ruff; release runbook; Gotcha 2 admin.html; Gotcha 4 dual price-history paths)
@.planning/STATE.md
@src/domain/parser.py
@src/domain/extractors/jsonld.py
@src/domain/service.py
@src/domain/scheduler.py
@src/domain/pricing.py
@src/domain/notifier.py
@src/mcp_server/server.py
@src/api/app.py
@src/api/admin.py  (trigger_price_check at ~L954–1091)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraction layer — enrich_with_llm, JSON-LD name sanity, OPENROUTER_API_KEY startup warning (items 1, 10, 11)</name>
  <files>src/domain/parser.py, src/domain/extractors/jsonld.py, src/api/app.py, tests/test_parser.py, tests/test_extractors.py</files>
  <action>
**(11) JSON-LD name sanity — src/domain/extractors/jsonld.py.** In `JsonLdExtractor.extract_from_html`, immediately after `_find_product` returns a node: when `product_name` is not None, compare case-insensitive alphanumeric tokens of length ≥ 3 (regex over lowercased strings, character class covering a-z, 0-9 and åäö; e.g. `re.findall` with `[a-z0-9åäö]{3,}` after `.lower()`) between `product_name` and the node's unescaped `name`. If BOTH token sets are non-empty and share NO token: `logger.warning` (mention both names) and return None so the cascade falls through to the LLM — this prevents recording a recommendation-carousel item's price with 0.95 confidence. When `product_name` is None, the JSON-LD name is empty, or any overlap exists → current behavior exactly. Do not try subsequent Product nodes (locked decision: fall through to LLM).

**(1) LLM enrichment method — src/domain/parser.py.** First refactor: extract the LLM cascade loop (the `for model_name in self.MODEL_CASCADE` block plus the below-threshold/floor tail of `extract_price`) into a private helper, e.g. `async def _run_llm_cascade(self, prompt, *, product_name, store_slug) -> PriceExtractionResult | None`, returning None instead of raising when all models fail; `extract_price` keeps its exact current external behavior (including the RuntimeError when everything fails AND there was no result — preserve by checking the helper's outcome, or keep `extract_price`'s tail as-is and have the helper only wrap the loop; executor's choice, but the cascade must exist ONCE).

Then add `async def enrich_with_llm(self, base: PriceExtractionResult, *, text_content: str, store_slug: str, product_name: str | None) -> PriceExtractionResult`:
- Builds the SAME prompt via `_build_prompt` over the already-fetched `text_content` — enrichment never fetches a page.
- Runs the cascade helper. If it raises, returns None, or the best result's `confidence < self.MIN_ACCEPT_CONFIDENCE` → log a warning and return `base` UNCHANGED. This method must NEVER propagate an exception (guard: enrichment failure never fails the check; the JSON-LD price is still recorded).
- Merge rule (locked): `base.price_sek` and `base.in_stock` always win. The LLM contributes ONLY fields `base` lacks (is None): `store_unit_price_sek`, `offer_price_sek`, `offer_type`, `offer_details`, `pack_size`, `package_amount`, `package_unit`. Build a NEW result via `dataclasses.replace(base, ...)` (PriceExtractionResult lives in src/domain/result.py) — do not mutate `base`. `confidence` stays `base.confidence` (JSON-LD remains the price authority).
- `raw_response`: keep the base dict (its `"source": "jsonld"` stays truthful) and add `"enriched": True` plus `"enrichment_confidence": <float>` — the scheduler's per-source stats key on `source`, so an enriched check still counts as jsonld.

**(10) Startup warning — src/api/app.py.** In `lifespan`, before `scheduler.start()`: if `os.getenv("OPENROUTER_API_KEY")` is falsy, log an ERROR (module logger; add one — app.py has none) stating LLM extraction and enrichment are effectively disabled until it is configured — mirroring the `MCP_BEARER_TOKEN not set` pattern in src/mcp_server/server.py. English text (developer-facing log). Do NOT crash and do not gate startup.

**Tests** (mock the LLM exactly like existing tests do — patch `_extract_with_model` / httpx; no network):
- tests/test_extractors.py: carousel-name mismatch → None + warning logged; token overlap → normal result; `product_name=None` → normal result; a name that only shares tokens shorter than 3 chars counts as no overlap.
- tests/test_parser.py: `enrich_with_llm` fills only None fields (base price/in_stock preserved even when the LLM disagrees); result below `MIN_ACCEPT_CONFIDENCE` → returns base unchanged; cascade exception → returns base unchanged, no raise; merged `raw_response` still has `source == "jsonld"` and `enriched is True`.
  </action>
  <verify>
    <automated>cd /home/magnus/dev/price-tracker && poetry run pytest tests/test_parser.py tests/test_extractors.py -q && poetry run ruff check src tests</automated>
  </verify>
  <done>`PriceParser.enrich_with_llm` exists with the merge/floor/never-raise semantics above; the LLM cascade loop exists exactly once; JsonLdExtractor returns None with a warning on zero token overlap and is unchanged otherwise; app startup logs an error (and continues) without OPENROUTER_API_KEY; new tests green.</done>
</task>

<task type="auto">
  <name>Task 2: One check flow + scheduler retry/weekday fixes + weekly summary + deals window (items 2, 3, 4, 6 + enrichment trigger wiring)</name>
  <files>src/domain/service.py, src/domain/scheduler.py, src/api/admin.py, src/domain/notifier.py, tests/test_service.py, tests/test_scheduler.py, tests/test_api.py</files>
  <action>
**(6) Consolidate — src/domain/service.py.** DELETE `PriceTrackerService.check_price` (zero production callers — the MCP check_price tool only reads history; its "IPriceTracker protocol" docstring claim is stale, no such protocol exists). Update the two stale comment references: src/api/admin.py ~L1025 ("scheduler and service.check_price are the others") and tests/test_api.py ~L933. Remove its tests from tests/test_service.py.

Add ONE module-level flow in service.py, e.g. `async def perform_price_check(*, product_store, product, store, session, fetcher, parser) -> ...` returning a small outcome (dataclass or dict) carrying: success flag, failure reason (`"fetch_failed"` / `"no_price"` / None), the (possibly enriched) extraction, the created PricePoint or None, and the mismatch message. Flow:
1. `fetcher.fetch(product_store.store_url)`; not ok (or no text) → outcome fetch_failed (carry the fetch error string for the admin 502).
2. `parser.extract_price(text_content=…, store_slug=store.slug, product_name=product.name, store_url=…, html_content=fetch_result.get("html"))`.
3. **Enrichment triggers (wires item 1, locked decision).** Only when `extraction.price_sek is not None` AND `extraction.raw_response.get("source") == "jsonld"` (API and LLM paths already carry all fields; discarded results have no price):
   - One query for the link's latest prior PricePoint: filter `PricePoint.product_store_id == product_store.id`, order `checked_at` desc, limit 1. NEVER resolve the link by the (product_id, store_id) pair — the AST gate in tests/test_static_gates.py fails that.
   - Trigger (a) first success: prior point is None, OR (`product_store.package_quantity is None` and `product_store.scraped_package_quantity is None`).
   - Trigger (b) drop: prior point exists and the current effective price (extraction offer or regular — for jsonld that is `price_sek`) < prior effective price (`offer_price_sek or price_sek` of the prior point).
   - Either fires → `extraction = await parser.enrich_with_llm(extraction, text_content=…, store_slug=…, product_name=…)`. Never raises (Task 1 guard), so the JSON-LD price is recorded regardless.
4. `extraction.price_sek is None` → outcome no_price (carry confidence).
5. `apply_scrape_to_link(product_store, extraction, product.unit)` — runs AFTER enrichment so harvested package fields feed the D-07 autofill; log the mismatch as today. This is now the ONLY scrape write path.
6. Build the PricePoint exactly as the scheduler does today (price/store_unit_price/offer_*/in_stock/raw_data=extraction.raw_response/checked_at naive UTC), `session.add`, set `product_store.last_checked_at` (preserves what record_price did for the manual path; the scheduler's own UPDATE harmlessly overwrites it). Do NOT commit — the caller owns the transaction.

**Scheduler rewiring — src/domain/scheduler.py.** `_check_single_product` becomes a thin wrapper: call `perform_price_check` with `self.fetcher`/`self.parser`, keep the stats bookkeeping (checks_total; checks_api/jsonld/llm keyed on `raw_response["source"]` — an enriched jsonld check still counts as checks_jsonld; checks_success), keep the mismatch log, call `_check_alerts` only when a point was recorded, and RETURN the outcome (the loop needs it).

**(2) Retry-loop fix — `_check_due_products`.**
- Exception path: in the existing `except` block, additionally advance the schedule with a backoff via its OWN short session: `UPDATE ProductStore SET next_check_at = now + 1 hour` for that id (leave last_checked_at alone), wrapped in its own try/except so a dead DB cannot kill the loop. Without this the link stays first in the ASC queue and hammers the store every 5 minutes.
- Failed-but-no-exception path: use the outcome. When the check FAILED (fetch_failed or no_price) and `product_store.check_weekday is not None` → set `next_check_at = now + 24h` instead of `_compute_next_check` (which would push a full week out). Frequency-based links keep current jittered scheduling on failure. SUCCESS keeps current behavior exactly (weekday morning-spread / jittered frequency).

**(3) Weekly summary — `_check_weekly_summary`.**
- (a) DELETE the "restart guard" block (the recent-alert-within-10h query and its early return, currently ~L408–424). Keep only the in-memory `_last_summary_date` dedup. Accept the rare duplicate after a Monday-afternoon restart (locked).
- (b) "Lägsta pris" per watched product is currently `ORDER BY checked_at DESC LIMIT 1` across ALL links — the latest point on the most-recently-checked link, not a lowest anything. Replace with lowest kr/enhet: latest point PER LINK (max(checked_at) per product_store_id subquery, same shape as `service.get_links_for_product`), joined to ProductStore + Store for the product; per link compute `unit_price_py(effective, package_quantity)` (effective = offer or regular; import from domain.pricing — pricing.py is THE definition, never write a second one); pick the minimum. Fallback when NO link has a quantity: lowest absolute effective price. Each watched row gains a price label key (e.g. `price_label`: `"kr/st"` style using the product's canonical unit for the unit-price case, plain `"kr"` for the absolute fallback) and `_build_summary_html` in src/domain/notifier.py renders that per-row label instead of the hardcoded `kr` in the watched-products rows — Swedish copy stays Swedish (diacritics are Task 3's job; do not fight over the same strings here).

**(4) Deals window — `service.get_current_deals`.** Replace the fixed 24h cutoff: select the LATEST price point per link (max(checked_at) per product_store_id subquery joined back on product_store_id + checked_at), keep only rows where that latest point has `offer_price_sek IS NOT NULL` and `checked_at >=` now − 7 days (staleness bound). Latest-per-link join means a superseded offer (newer point without offer on the same link) can never appear, and the `seen`-set dedupe becomes redundant — remove it. Ordering stays recency-based; keep/adjust the docstring note saying that is deliberate.

**Admin endpoint — src/api/admin.py `trigger_price_check` (~L954).** Replace the inline fetch/parse/apply/record block with a `perform_price_check` call on the request `session`, then commit (and refresh the point if needed for response values). BEHAVIOR PARITY with today's HTTP contract: 400 on invalid UUID, 404 when the link row is missing, 502 with the fetch error on fetch failure, 500 wrapper on unexpected errors; the no-price JSON shape keeps keys message/confidence/price_sek/offer_price_sek; the success JSON keeps exactly today's keys: message, price_sek, unit_price_sek (computed via the existing `_computed_unit_price` + `_effective_price` from the possibly-autofilled quantity), store_unit_price_sek, package_quantity, quantity_mismatch, offer_price_sek, offer_type, in_stock, confidence.

**Tests.**
- tests/test_service.py: perform_price_check with mocked fetcher/parser — success records a point and applies scrape; fetch-fail and no-price outcomes; trigger (a) fires on a quantity-less link with no history; trigger (b) fires on an effective-price drop and NOT on an unchanged/raised price; no enrichment when source is not jsonld. Remove the dead check_price tests.
- tests/test_scheduler.py: exception in the check advances next_check_at by ~1h via a separate UPDATE; failed weekday check reschedules +24h; successful weekday check keeps next-weekday behavior; restart-guard test deleted/updated; weekly summary lowest-kr/enhet incl. the absolute fallback + label.
- tests/test_api.py: trigger endpoint returns the same JSON keys as before (both shapes); fix the stale comment at ~L933.
- tests/test_static_gates.py must stay green (never resolve a link by the (product_id, store_id) pair).
  </action>
  <verify>
    <automated>cd /home/magnus/dev/price-tracker && poetry run pytest tests/test_service.py tests/test_scheduler.py tests/test_api.py tests/test_static_gates.py -q && poetry run ruff check src tests</automated>
  </verify>
  <done>`service.check_price` is gone; scheduler and admin trigger both call the single `perform_price_check` (which owns the enrichment triggers); an exception-throwing link is rescheduled +1h and a failed weekday check +24h; weekly summary has no restart guard and reports lowest kr/enhet with labeled fallback; deals are latest-per-link within 7 days; admin endpoint responses byte-compatible on keys; static AST gate green.</done>
</task>

<task type="auto">
  <name>Task 3: MCP list_products + check_price docstring, watch-form cleanup, email diacritics, release v0.4.0 (items 5, 7, 8, 9)</name>
  <files>src/mcp_server/server.py, src/domain/service.py, src/api/templates/admin.html, src/domain/notifier.py, pyproject.toml, tests/test_mcp.py, tests/test_notifier.py, tests/test_email.py</files>
  <action>
**(5) MCP list_products — src/mcp_server/server.py + src/domain/service.py.** The tool reads `product.get("stores")` but `service.get_products` never returns that key, so every product prints "Ej länkad". Add a batched service helper (ONE query, no N+1), e.g. `async def get_store_names_by_product(self) -> dict[str, list[str]]`: join ProductStore→Store, select product_id + Store.name, dedupe names per product (a product can hold several links at one store post-04.1), sorted, keys `str(product_id)`. Rewrite the tool to use it; drop the now-dead `get_stores()`/`store_map` plumbing there. Output format unchanged: same markdown line shape, "Ej länkad" (Swedish, user-facing) when a product has no links.

**(7) MCP check_price docstring — server.py.** Rewrite the docstring (it IS the tool description agents read; English, developer-facing) to state explicitly that it returns the LAST OBSERVED price per tracked listing from stored history and does NOT trigger a live fetch/scrape — so an agent never believes it just ran a fresh check. Keep the compare_stores cross-reference and the Swedish output strings as-is.

**(8) Watch form — src/api/templates/admin.html.** LOCKED: steer NEW watches to kr/enhet targets. In the `form-create-watch` modal, remove the whole "Målpris (kr)" form-group (label + its number input, currently ~L238–241). In `handleCreateWatch`'s payload builder (~L1352), keep the JSON key but send null unconditionally (API schema, existing watches, and scheduler alert logic stay UNTOUCHED). The watch-list rendering of existing target prices (~L679) and the "Mål för jämförpris (kr)" input stay exactly as-is. Gotcha 2: admin.html is 3 fragments split on SECTION_SEPARATOR reassembled at request time — verify the JS with the awk + node incantation below and keep the `test -s` guard.
<!-- planner-discipline-allow: target_price_sek -->

**(9) Email diacritics — src/domain/notifier.py.** Emails are UTF-8 HTML (charset declared) — full Swedish is safe. Replace: "Ditt malpris:" → "Ditt målpris:"; "Jamforelsepris:" → "Jämförelsepris:"; "Jamforelsepris-fall:" → "Jämförelsepris-fall:"; "under ordinarie jamforelsepris" → "under ordinarie jämförelsepris"; subject "Veckans prisoversikt - Prisspaning" → "Veckans prisöversikt – Prisspaning" (en dash); h2 "Veckans prisoversikt" → "Veckans prisöversikt"; "Har ar en sammanfattning av priser och erbjudanden denna vecka." → "Här är en sammanfattning av priser och erbjudanden denna vecka."; "Du far detta for att du bevakar produkten." → "Du får detta för att du bevakar produkten."; watched-table header "Lagsta pris" → "Lägsta pris". "Prisvarning" is already correct; ASCII examples inside code comments may stay. Update any assertions in tests/test_notifier.py and tests/test_email.py that pin the old ASCII strings.

**Tests.** tests/test_mcp.py: list_products shows the linked store names from the batched helper and "Ej länkad" only for genuinely unlinked products; check_price docstring/tool description mentions it is history-only (assert on the tool description if the harness exposes it, else skip that assertion). tests/test_notifier.py / tests/test_email.py updated to the new Swedish strings.

**Release v0.4.0 (per CLAUDE.md runbook — shipping is part of the task).** NO schema change is included, so no migration/volume concerns (Gotcha 3 does not apply).
1. Full gate: `poetry run ruff check src tests && poetry run ruff format --check src tests && poetry run pytest -q` (expect 205 passed + 12 skipped without a reachable DB, plus this task set's new tests).
2. Bump `version` in pyproject.toml 0.3.3 → 0.4.0 and commit the bump (the version in pyproject IS the tag).
3. `git push origin main` — never tag a dirty tree or an un-pushed commit.
4. `git tag v0.4.0 && git push origin v0.4.0`.
5. Watch the build: `gh run watch "$(gh run list --workflow=release.yml --limit=1 --json databaseId -q '.[0].databaseId')"` — a tag that fails to build is not a release.
6. Report to Magnus that v0.4.0 is built and needs the deploy bump in home-server's `compose/dokploy-apps/price-tracker/docker-compose.yml` — that repo is NOT this repo; do not edit it from here. The orchestrator handles the final docs commit afterwards (docs-only rides along, no new tag).
  </action>
  <verify>
    <automated>cd /home/magnus/dev/price-tracker && poetry run ruff check src tests && poetry run ruff format --check src tests && poetry run pytest -q && awk '/SECTION_SEPARATOR/{n++;next} n==2' src/api/templates/admin.html > /tmp/admin.js && test -s /tmp/admin.js && node --check /tmp/admin.js && [ "$(awk '/form-create-watch/,/<\/form>/' src/api/templates/admin.html | grep -c 'target_price_sek')" -eq 0 ] && grep -q 'Veckans prisöversikt' src/domain/notifier.py</automated>
  </verify>
  <done>list_products prints real store names via one batched query; check_price docstring says history-only; the create-watch form has no absolute target-price input (region-scoped check green) while list display and API stay intact; notifier templates carry proper Swedish diacritics and tests assert the new strings; full suite + ruff green; pyproject at 0.4.0; v0.4.0 tag pushed and the release build watched to success.</done>
</task>

</tasks>

<verification>
- `poetry run ruff check src tests && poetry run ruff format --check src tests && poetry run pytest -q` — everything green (Postgres integration tier skips cleanly without a DB; run the full suite anyway).
- `tests/test_static_gates.py` green — no query in src/ resolves a link by the (product_id, store_id) pair.
- admin.html JS fragment parses via the awk + `test -s` + `node --check` incantation (Gotcha 2). "The file parses" is not "the page renders" — the executor should also spot-check that the create-watch modal fragment is structurally intact HTML.
- Exactly ONE fetch→extract→record implementation remains in src/ (service.perform_price_check); scheduler and admin both route through it.
- `gh run watch` confirms the v0.4.0 release build succeeded.
</verification>

<success_criteria>
All eleven flow-analysis findings addressed exactly as locked: enrichment at the two triggers only (first success / price drop), JSON-LD price authority preserved, enrichment never fails a check; scheduler backoff +1h on exception and +24h weekday-failure retry; weekly summary restart guard deleted and lowest kr/enhet computed via domain.pricing; deals = latest-per-link ≤ 7 days; MCP list_products fixed batched; check_price docstring truthful; absolute target-price input removed from the create form only; Swedish diacritics restored; startup warning added; suite green; v0.4.0 tagged, pushed, built.
</success_criteria>

<output>
On completion the orchestrator writes `.planning/quick/260714-x48-flodesanalys-atgarder-llm-berikning-sche/SUMMARY.md` and the STATE.md docs commit (docs-only — rides along, no extra tag).
</output>
