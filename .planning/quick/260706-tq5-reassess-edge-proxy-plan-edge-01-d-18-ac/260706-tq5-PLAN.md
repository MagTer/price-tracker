---
quick_id: 260706-tq5
slug: reassess-edge-proxy-plan-edge-01-d-18-ac
type: execute
autonomous: true
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
must_haves:
  truths:
    - "PROJECT.md, REQUIREMENTS.md, ROADMAP.md, and STATE.md describe the edge-proxy/ingress hosting target as Dokploy-managed, not a standalone Flatcar Container Linux VM in a Proxmox mini-PC"
    - "The Traefik + auth-middleware ingress architecture (oauth2-proxy-style X-Auth-Request-Email header injection) and the IAP header-trust auth mechanism are unchanged — only the hosting/ownership description changed"
    - "A new decision D-20 (2026-07-06) is recorded in PROJECT.md's changelog, REQUIREMENTS.md's changelog, and STATE.md's decision log, without renumbering any existing D-01..D-19 references"
    - "ROADMAP.md's Phase 1-4 checkmarks, phase goals, success criteria, and requirement mappings are byte-identical to before this task — only the Auth topology note (~line 7) changed"
    - "CLAUDE.md and EXTRACTION.md are not modified by this task"
  artifacts:
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "PROJECT.md Key Decisions table row (Edge proxy / portal stack repo) -> STATE.md decision log (both cite D-18 original lock + D-20 hosting reassess)"
    - "ROADMAP.md Auth topology note -> REQUIREMENTS.md EDGE-01 -> STATE.md Deferred Items EDGE-01 row (all three describe the same Dokploy-managed ingress, consistently)"
---

<objective>
Reassess the edge-proxy/ingress plan (backlog item EDGE-01, locked by decision D-18 on 2026-05-02, previously reassessed as D-19 on 2026-05-04) across the 4 core planning artifacts: `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.

These 4 files currently describe the ingress hosting target as "Traefik + oauth2-proxy + Homepage dashboard on Flatcar Container Linux inside a Proxmox VM (single mini-PC)" — framed as a wholly separate, hand-built repo/codebase on a standalone hosting target, explicitly out of scope for the price-tracker milestone.

The user clarified (2026-07-06) that this hosting framing is stale: the deployment target for price-tracker itself is Dokploy, and the Entra-auth-enforcing ingress stack (Traefik + auth middleware, architecturally the same as before — oauth2-proxy-style `X-Auth-Request-Email` header injection) will be operated **within Dokploy's managed scope**, not hand-built on a standalone Flatcar/Proxmox VM. This is a hosting/ownership correction only — the architecture itself ("is still true, just managed in the Dokploy scope") and the IAP header-trust auth mechanism are unchanged. The ingress is not yet built, pending Entra client registration (unchanged fact, not new information).

This is narrower than the 3 prior quick tasks on this repo today (260706-rso, 260706-t3p, 260706-tha), which fixed code bugs and corrected CLAUDE.md's auth-mechanism description. This task does NOT touch the auth mechanism, the MCP subdomain decision, or any phase's scope/gates/checkmarks — it only corrects *where the ingress lives* in 4 planning-artifact locations, using decision ID **D-20** (highest existing ID across these 4 files is D-19, confirmed via grep).

Do NOT touch `CLAUDE.md` (already corrected by quick task 260706-tha) or `EXTRACTION.md`. Do NOT renumber any existing D-01..D-19 reference. Do NOT change Phase numbering, phase completion checkmarks, or success criteria of Phases 1-4 in ROADMAP.md.

Purpose: keep planning artifacts truthful about where the Entra-auth-enforcing ingress will actually be operated, so future phase planning (Phase 5 and beyond) doesn't re-derive a stale "separate hand-built VM stack" assumption.

Output: 4 files edited with a consistent Dokploy-managed-ingress framing and a new D-20 decision-log/changelog entry in each; a documented judgment call on how repo-wide historical mentions of Flatcar/Proxmox were handled.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reassess edge-proxy hosting in PROJECT.md (D-20)</name>
  <files>.planning/PROJECT.md</files>
  <action>
Make exactly 2 edits to `.planning/PROJECT.md`. Do not touch any other line.

**Edit 1 — Key Decisions table row (~line 97).** Replace the exact row:

`| Edge proxy / portal stack repo | Belongs out of the price-tracker extraction milestone; separate codebase, separate hosting target (Flatcar VM), separate concerns (TLS, OIDC, routing, dashboard) | Locked 2026-05-02 (D-18) — separate future GSD milestone or repo: Traefik + oauth2-proxy + Homepage on Flatcar |`

with:

`| Edge proxy / portal stack repo | Belongs out of the price-tracker extraction milestone; separate codebase, separate concerns (TLS, OIDC, routing, dashboard); ingress is operated within Dokploy's managed scope, not a standalone hosting target | Locked 2026-05-02 (D-18) — Traefik + oauth2-proxy + Homepage; reassessed 2026-07-06 (D-20) — managed within Dokploy's scope, not a hand-built stack on a standalone Flatcar/Proxmox VM |`

Preserve the row label ("Edge proxy / portal stack repo") and the "Traefik + oauth2-proxy + Homepage" architecture description unchanged — only the hosting-target clause and the Outcome cell's closing description change.

**Edit 2 — changelog line at end of file (~line 117).** Keep the existing `*Last updated: 2026-05-04 — D-19 roadmap reassess...*` line exactly as-is (it is a historical record of that reassess), and append a new line directly below it:

`*Last updated: 2026-07-06 — D-20 reassess: edge-proxy ingress hosting target corrected from a standalone Flatcar/Proxmox VM to Dokploy-managed ingress (Traefik + auth-middleware architecture and D-18 lock unchanged; still out of the price-tracker extraction milestone's build scope)*`

Do not modify the "What This Is", Requirements, Constraints, or any other section.
  </action>
  <verify>
    <automated>test "$(grep -c 'Flatcar\|Proxmox' .planning/PROJECT.md)" -eq 0 && grep -q "D-20" .planning/PROJECT.md && grep -qi "Dokploy" .planning/PROJECT.md && grep -q "D-19 roadmap reassess" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md's Key Decisions table row and end-of-file changelog describe Dokploy-managed ingress hosting with a new D-20 citation; the D-19 changelog line and all other sections are untouched; zero remaining "Flatcar"/"Proxmox" mentions in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Reassess EDGE-01 hosting in REQUIREMENTS.md (D-20)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make exactly 2 edits to `.planning/REQUIREMENTS.md`. Do not touch any other line (do not touch AUTH-01..04, MCP-05, DEPLOY-01/03/04, DB-03, TEST-02, or the Traceability table — those are correct and out of scope).

**Edit 1 — EDGE-01 backlog item (~line 105).** Replace the exact line:

`- **EDGE-01**: Traefik + oauth2-proxy + Homepage dashboard on Flatcar Container Linux inside a Proxmox VM (single mini-PC). Owns: TLS termination (Let's Encrypt DNS-01 wildcard), Entra OIDC, routing to backend apps, landing-page tiles. Backend apps (price-tracker, future ai-agent-platform consolidation, etc.) join a shared `edge` docker network and trust `X-Auth-Request-Email` headers (D-18 in `01-CONTEXT.md`). Subdomain strategy: `prices.<domain>`, `mcp.<domain>`, `agent.<domain>`. **Does NOT belong inside the price-tracker extraction milestone** — should be its own GSD milestone or repo.`

with:

`- **EDGE-01**: Traefik + oauth2-proxy + Homepage dashboard ingress, operated within Dokploy's managed scope (not a standalone hand-built stack). Owns: TLS termination (Let's Encrypt DNS-01 wildcard), Entra OIDC, routing to backend apps, landing-page tiles. Backend apps (price-tracker, future ai-agent-platform consolidation, etc.) join a shared `edge` docker network and trust `X-Auth-Request-Email` headers (D-18 in `01-CONTEXT.md`; hosting reassessed 2026-07-06 per D-20). Subdomain strategy: `prices.<domain>`, `mcp.<domain>`, `agent.<domain>`. **Does NOT belong inside the price-tracker extraction milestone** — ingress is managed via Dokploy, not built as part of this repo.`

Preserve the section heading `### Edge Proxy / Portal Stack (separate milestone or repo)` above it unchanged.

**Edit 2 — changelog line at end of file (~line 178).** Keep the existing `*Last updated: 2026-05-04 — D-19 roadmap reassess...*` line exactly as-is, and append a new line directly below it:

`*Last updated: 2026-07-06 — D-20 reassess: EDGE-01 hosting corrected from a standalone Flatcar Container Linux VM in Proxmox to Dokploy-managed ingress (Traefik + oauth2-proxy architecture unchanged); still out of the price-tracker extraction milestone*`
  </action>
  <verify>
    <automated>test "$(grep -c 'Flatcar\|Proxmox' .planning/REQUIREMENTS.md)" -eq 0 && grep -q "D-20" .planning/REQUIREMENTS.md && grep -qi "Dokploy" .planning/REQUIREMENTS.md && grep -q "D-19 roadmap reassess" .planning/REQUIREMENTS.md && grep -q "AUTH-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md's EDGE-01 backlog item and end-of-file changelog describe Dokploy-managed ingress hosting with a new D-20 citation; the D-19 changelog line, AUTH-01..04, MCP-05, and all other requirements are untouched; zero remaining "Flatcar"/"Proxmox" mentions in this file.</done>
</task>

<task type="auto">
  <name>Task 3: Reassess auth-topology ingress attribution in ROADMAP.md (D-20)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Make exactly 1 edit to `.planning/ROADMAP.md`. Do not touch any other line — do not touch phase checkmarks, phase goals, success criteria, requirement lists, or the Progress table.

**Edit — Auth topology note (~line 7).** Replace the exact line:

`**Auth topology (locked 2026-05-02 in 01-CONTEXT.md, reassessed 2026-05-04):** This repo does NOT terminate Entra OIDC. An upstream Identity-Aware Proxy (Traefik + oauth2-proxy, lives in a separate edge-stack repo per D-18) terminates OIDC once at the edge and forwards `X-Auth-Request-Email` to the admin host. The MCP host is excluded from the IAP middleware so it can authenticate purely with `MCP_BEARER_TOKEN`. Phase 3 reads the header; Phase 4 wires the bearer-only subdomain.`

with:

`**Auth topology (locked 2026-05-02 in 01-CONTEXT.md, reassessed 2026-05-04; ingress hosting reassessed 2026-07-06 per D-20):** This repo does NOT terminate Entra OIDC. An upstream Identity-Aware Proxy (Traefik + oauth2-proxy, operated within Dokploy's managed scope per D-20) terminates OIDC once at the edge and forwards `X-Auth-Request-Email` to the admin host. The MCP host is excluded from the IAP middleware so it can authenticate purely with `MCP_BEARER_TOKEN`. Phase 3 reads the header; Phase 4 wires the bearer-only subdomain.`

Only the parenthetical topology label and the "lives in a separate edge-stack repo per D-18" clause change — the MCP host exclusion sentence and header-forwarding description are byte-identical before and after.
  </action>
  <verify>
    <automated>test "$(grep -c 'Flatcar\|Proxmox' .planning/ROADMAP.md)" -eq 0 && grep -q "D-20" .planning/ROADMAP.md && grep -qi "Dokploy" .planning/ROADMAP.md && grep -q "Traefik + oauth2-proxy" .planning/ROADMAP.md && grep -q "\[x\] \*\*Phase 4: MCP Server + Agent Wiring\*\*" .planning/ROADMAP.md && test "$(grep -c '\[x\]' .planning/ROADMAP.md)" -eq 9</automated>
  </verify>
  <done>ROADMAP.md's Auth topology note attributes the ingress to Dokploy-managed scope with a D-20 citation; the MCP host exclusion and header-forwarding sentence are unchanged; all 4 Phase 1-4 checkmarks and their 5 plan-level checkmarks (9 total "[x]" occurrences) are untouched; zero remaining "Flatcar"/"Proxmox" mentions in this file.</done>
</task>

<task type="auto">
  <name>Task 4: Reassess edge-proxy hosting in STATE.md (D-20)</name>
  <files>.planning/STATE.md</files>
  <action>
Make exactly 3 edits to `.planning/STATE.md`, in this order. Do not touch the "Current Position" section, the Quick Tasks Completed table, or Session Continuity — those are correct and out of scope.

**Edit 1 — D-19 decision-log bullet (~line 59), amend trailing clause and append a new D-20 bullet directly below it.** Replace the exact line:

`- 2026-05-04 D-19 reassess: Locked MCP subdomain (`mcp.<domain>`) over `/mcp` path because IAP auth-bypass is per-host. Locked IAP header trust (`X-Auth-Request-Email`) as the Phase 3 auth model — drops `fastapi-azure-auth`, `pyjwt`, `cryptography` from this repo permanently. Locked edge-proxy stack as separate future milestone (does NOT belong inside extraction milestone).`

with these two lines:

`- 2026-05-04 D-19 reassess: Locked MCP subdomain (`mcp.<domain>`) over `/mcp` path because IAP auth-bypass is per-host. Locked IAP header trust (`X-Auth-Request-Email`) as the Phase 3 auth model — drops `fastapi-azure-auth`, `pyjwt`, `cryptography` from this repo permanently. Locked edge-proxy stack as out of the price-tracker extraction milestone (does NOT belong inside extraction milestone; hosting target corrected 2026-07-06 — see D-20 below).`
`- 2026-07-06 D-20 reassess: Corrected edge-proxy ingress hosting description from a standalone Flatcar Container Linux VM in Proxmox to Dokploy-managed ingress. Architecture unchanged — still Traefik + auth-middleware (oauth2-proxy-style header injection) terminating Entra OIDC and forwarding `X-Auth-Request-Email`; only the hosting/ownership model changed (Dokploy manages it, not a hand-built separate-repo stack). EDGE-01 remains out of the price-tracker extraction milestone's build scope; ingress is not yet built, pending Entra client registration.`

**Edit 2 — Blockers/Concerns bullet (~lines 69-70).** Replace the exact line:

`- **Edge-proxy / portal stack** is deferred to a separate future GSD milestone or repo (D-18, EDGE-01). Phases 3 + 4 ASSUME the IAP exists; if it does not exist when Phase 3 lands, Phase 3 still ships behind a "trust the header" dependency and the operator runs the app in a private network until the edge stack is built.`

with:

`- **Edge-proxy / portal stack** is operated within Dokploy's managed scope, out of this repo's build scope (D-18, EDGE-01; hosting reassessed 2026-07-06 per D-20). Phases 3 + 4 ASSUME the IAP exists; if it does not exist when Phase 3 lands, Phase 3 still ships behind a "trust the header" dependency and the operator runs the app in a private network until the Dokploy-managed ingress is built (pending Entra client registration).`

**Edit 3 — Deferred Items table EDGE-01 row (~line 91).** Replace the exact line:

`| Edge proxy / portal | EDGE-01 (Traefik + oauth2-proxy + Homepage on Flatcar — separate future milestone or repo) | Out of milestone | 2026-05-04 (D-19 reassess) |`

with:

`| Edge proxy / portal | EDGE-01 (Traefik + oauth2-proxy + Homepage, operated within Dokploy's managed scope) | Out of milestone | 2026-05-04 (D-19 reassess); hosting corrected 2026-07-06 (D-20) |`
  </action>
  <verify>
    <automated>test "$(grep -c 'Flatcar\|Proxmox' .planning/STATE.md)" -eq 0 && grep -q "D-20" .planning/STATE.md && grep -qi "Dokploy" .planning/STATE.md && grep -q "D-19 reassess" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md's D-19 decision-log bullet is amended and a new D-20 bullet added below it; the Blockers/Concerns bullet and Deferred Items EDGE-01 row both describe Dokploy-managed ingress; zero remaining "Flatcar"/"Proxmox" mentions in this file.</done>
</task>

<task type="auto">
  <name>Task 5: Repo-wide verification + judgment call on historical mentions</name>
  <files>(none — verification only, no files modified)</files>
  <action>
Run `grep -rn "Flatcar\|Proxmox" .planning/` across the whole planning tree.

Confirm zero matches remain in the 4 target files edited by Tasks 1-4 (`.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`).

Expect exactly 2 residual matches, both inside `.planning/phases/01-skeleton-domain-copy/` — `01-CONTEXT.md` (locked Phase 1 discussion decision record) and `01-DISCUSSION-LOG.md` (a verbatim discussion transcript). These are historical, already-closed Phase 1 artifacts documenting what was decided/discussed *at the time* (2026-05-02) — not living planning state. Rewriting them would falsify the historical record of that discussion and is explicitly out of scope per this task's instructions (only PROJECT.md, REQUIREMENTS.md, ROADMAP.md, and STATE.md are in scope).

**Judgment call:** leave these 2 historical files untouched rather than scrubbing them, and record this reasoning in the SUMMARY.md (full scrub applied to the 4 living planning-state files; the 2 immutable historical discussion/context artifacts from the original Phase 1 discussion are intentionally left as-is since they are a record of what was true when that discussion happened, not current state).

Also confirm `CLAUDE.md` and `EXTRACTION.md` were not modified by this task (both are explicitly out of scope).
  </action>
  <verify>
    <automated>test "$(grep -rln 'Flatcar\|Proxmox' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md 2>/dev/null | wc -l)" -eq 0 && test "$(grep -rl 'Flatcar\|Proxmox' .planning/ | grep -v '^\.planning/phases/01-skeleton-domain-copy/' | wc -l)" -eq 0 && ! git status --porcelain -- CLAUDE.md EXTRACTION.md | grep -q .</automated>
  </verify>
  <done>Repo-wide grep for "Flatcar"/"Proxmox" returns zero matches in the 4 living planning files and exactly the 2 expected historical Phase 1 artifacts elsewhere; CLAUDE.md and EXTRACTION.md are unmodified; the judgment call (full scrub of living state, marked-historical retention for closed Phase 1 discussion artifacts) is documented in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `grep -c "Flatcar\|Proxmox" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` each return 0.
- `grep -l "D-20" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` lists all 4 files.
- `grep -qi "Dokploy"` succeeds in all 4 files.
- No existing `D-01`..`D-19` reference was renumbered (spot-check: `grep -c "D-18" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` each return >= 1, unchanged count from before this task).
- `git diff --stat` touches only `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` — no other file, and specifically not `CLAUDE.md` or `EXTRACTION.md`.
- ROADMAP.md's Phase 1-4 checkmarks (`- [x]`) and Phase 5 unchecked box (`- [ ]`) are unchanged; success criteria and requirement lists for all 5 phases are byte-identical to before this task.
</verification>

<success_criteria>
- All 4 core planning artifacts consistently describe the edge-proxy/ingress as operated within Dokploy's managed scope, correcting the stale "Flatcar Container Linux VM in a Proxmox mini-PC" hosting framing.
- The underlying architecture (Traefik + oauth2-proxy-style header injection terminating Entra OIDC, forwarding `X-Auth-Request-Email`) and the IAP header-trust auth mechanism are explicitly unchanged — this was a hosting/ownership correction, not an architecture change.
- New decision D-20 (2026-07-06) is recorded in PROJECT.md, REQUIREMENTS.md, and STATE.md changelogs/decision logs, alongside (not replacing) the existing D-18/D-19 history.
- ROADMAP.md's phase scope, gates, and completion status for Phases 1-4 are untouched.
- CLAUDE.md and EXTRACTION.md are untouched.
- The judgment call on repo-wide historical Flatcar/Proxmox mentions (full scrub of the 4 living files; marked-historical retention in closed Phase 1 discussion artifacts) is documented in the SUMMARY.
</success_criteria>

<output>
Create `.planning/quick/260706-tq5-reassess-edge-proxy-plan-edge-01-d-18-ac/260706-tq5-SUMMARY.md` when done, with `status: complete` in its frontmatter.
</output>