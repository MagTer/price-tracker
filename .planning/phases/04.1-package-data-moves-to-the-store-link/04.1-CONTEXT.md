# Phase 04.1: Package data moves to the store link - Context

**Gathered:** 2026-07-14
**Status:** Ready for planning

<domain>
## Phase Boundary

Reshape the data model so the **link owns the package data it actually describes**:
`package_size` + `package_quantity` move from `Product` to `ProductStore`. `unit` stays
on `Product`.

- **Product** = the abstract good — name, brand, category, **unit** (the comparison unit:
  st / liter / kg — the property that makes variants comparable)
- **ProductStore (link)** = the concrete package listing — store_url, **package_quantity**,
  **package_size** (display label), scheduling fields as today
- **PricePoint** = the observed price at a link (see D-04: unit price is no longer stored)

This unlocks the app's core question — *"cheapest kr/rulle for Lambi across pack sizes and
stores"* — which today's model cannot answer, because three pack sizes are three unrelated
products with no grouping key.

**In scope:** the model move, the constraint change it forces, the unit-price calculation,
the scraper's new quantity-verification role, the admin dialogs, the product page, the
MCP `compare_stores` shape, the migration.

**Not in scope:** a middle `Variant` entity (considered and deliberately rejected — see
`.planning/SEED-package-data-moves-to-link.md`; do not resurrect without a new reason).

</domain>

<decisions>
## Implementation Decisions

### Link identity and uniqueness

- **D-01:** **Drop `uq_product_store(product_id, store_id)`; make `store_url` globally
  UNIQUE instead.** A link *is* a store page, so the URL is its natural key.
  **This is a blocker, not a nicety:** the existing constraint permits only one link per
  product per store, which forbids exactly the thing this phase exists to enable
  ("Lambi 24-pack at Willys" + "Lambi 8-pack at Willys"). Global uniqueness on `store_url`
  additionally prevents the same page being tracked twice or attached to two different
  products.
  - `(product_id, store_id)` → no constraint. Same URL twice → blocked.

- **D-02:** **`package_quantity` stays NULLABLE on the link** — so a link can be saved
  before its pack size is known. But it is **never silently blank**: the first successful
  scrape autofills it (D-07), and until then the product page renders the link as
  `kr/unit — needs amount`. Nullable in the schema, visibly flagged in the UI.

### Unit price — source of truth

- **D-03:** **Unit price is COMPUTED, never scraped:** `price_sek / link.package_quantity`.
  One definition by construction, so kr/unit is comparable across every store and pack
  size — which is the entire point of the phase.
  **Context the seed omits:** today `Product.package_quantity` is *never used in any
  arithmetic*. `PricePoint.unit_price_sek` is 100% LLM-parser output (`parser.py:261-266`
  scrapes the page's printed unit price, falling back to `price / pack_size`). Stores print
  unit prices with **incompatible definitions** (kr/rulle vs kr/pack vs kr/100g), which is
  why the scraped number cannot answer a cross-store comparison. **So this phase CHANGES
  the source of truth for unit price — it is not merely relocating a calculation.**

- **D-04:** **Compute on read; do not persist unit price.** `price_sek` is the observed
  fact; unit price is a *view* of it. Derive at display/compare time.
  → **`PricePoint.unit_price_sek` is DROPPED.** Correcting a link's quantity (24 → 12)
  retroactively fixes all history for free, and the stale-snapshot class of bug cannot
  exist.

- **D-05:** **Add `PricePoint.store_unit_price_sek`** — the store's *printed* unit price,
  as scraped, clearly labeled "what the store claims". Displayed **beside** the computed
  kr/unit, **never sorted on** (its definition varies per store). It is a raw signal for
  spotting mismatches, and feeds the quantity cross-check.

  Product page shape:

  | Link | Price | kr/rulle *(computed, sortable)* | Store says *(raw)* |
  |---|---|---|---|
  | Willys 24-p | 139.90 | 5.83 | 5.83/rulle |
  | ICA 8-p | 59.90 | 7.49 | 7.49/rulle |
  | Coop 24-p | 129.00 | 5.38 | 12.90/100g ← different unit |

### Scraper — role inverts from answer to evidence

- **D-06:** The scrape **stops being the output (unit price) and becomes the input
  (quantity)**. A page fetch yields two quantity signals: `parser.pack_size` from the title
  ("16-p"), and a back-derivation from the printed unit price (`price / store_unit_price`).
  This is what makes AC#4 ("scraper fills/verifies link quantity") real.

- **D-07:** **Autofill when empty; flag on conflict; NEVER overwrite.**
  - stored quantity is NULL → scrape fills it silently
  - stored quantity is set and the page disagrees → surface a visible flag on the link
    ("page says 12, you have 24"); leave the stored value alone
  - **Rationale:** the typed value is *intent*; the page is *evidence*. Evidence does not
    get to silently rewrite intent — especially now that kr/unit and the whole history view
    are computed from that number (D-03/D-04), so one bad LLM extraction would silently
    corrupt the comparison.

- **D-08:** **Extend the parser contract** so non-countable products can be verified too.
  `PriceExtractionResult` gains `package_amount: Decimal | None` and
  `package_unit: str | None` alongside the existing `pack_size: int | None`.
  **Why:** `pack_size` is an int ("items in a pack") — meaningful for `st` (24 rolls, 60
  capsules) but useless for a 0.5-liter bottle or a 400g bag, and groceries are full of
  liter/kg goods. Normalize `package_unit` → the product's canonical unit using the **same
  st/liter/kg factors the admin UI already has** (`PKG_UNITS` in `admin.html`, ml→liter,
  g→kg).

- **D-09:** **Store the page's reading in `ProductStore.scraped_package_quantity`**
  (nullable). Every check writes it. The UI flags the link whenever it differs from
  `package_quantity`. No state machine, no `resolved_at` bookkeeping — the flag
  **self-clears** the moment either number is corrected, and the last page reading stays
  inspectable.

### UI

- **D-10:** **Product dialog:** name / brand / category / **unit only**. No package fields.
  Unit is a dropdown of canonical units (st / liter / kg).

- **D-11:** **Link dialog gains the packaging chain** — "amount in package" + auto-suggested
  editable label. The v0.2.1 guided chain **moves** from the product dialog to the link
  dialog (commit `6c2347f`, `admin.html`). It needs **no unit selector**: the product
  already knows its unit, so the link renders the amount field *in the product's unit*.
  Entry helpers (ml/g) may remain, normalizing to the canonical unit as v0.2.1 already does.

- **D-12:** **Product page** lists all links with package label, current price, and
  **kr/unit — sortable**. This is the screen that answers the toilet-paper question.

### MCP

- **D-13:** **`compare_stores` returns one row per LINK, sorted by kr/unit** (store +
  package + price + kr/unit), replacing today's one-row-per-store table. The agent and the
  UI then tell the same story, and "cheapest per unit" becomes answerable over MCP — the
  `priser` skill is the platform's main consumer.

### Migration

- **D-14:** **Rewrite `0001_initial.py` in place** (single clean migration) rather than
  stacking a 0002. Sanctioned by the seed; the DB is empty, so there is nothing to rescue.

- **D-15:** **⚠ MANDATORY consequence of D-14 — the DB reset is a required, named step,
  not an afterthought.** The deployed v0.2.0 instance's DB is **already stamped at revision
  0001**. If 0001 is rewritten without resetting, `alembic upgrade head` sees `0001 == head`
  and **applies NOTHING** — the app then runs against the OLD schema. The plan MUST include:

  ```bash
  docker compose down -v          # drops the postgres volume (tables + alembic_version)
  docker compose up -d postgres
  alembic upgrade head            # rewritten 0001
  ```

### Claude's Discretion

- Exact sort/empty-state presentation of the product page link table (beyond "sortable by
  kr/unit" and the "needs amount" flag).
- Precise wording/placement of the mismatch flag.
- Whether the quantity back-derivation from `store_unit_price_sek` is used as a second
  autofill source or only as a cross-check — D-07's never-overwrite rule governs either way.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The decision itself
- `.planning/SEED-package-data-moves-to-link.md` — the originating brief: the decision, the
  operator's rationale, the **rejected `Variant` alternative** (do not resurrect), the
  touch-point inventory, and the 5 acceptance criteria. **Read first.**
- `.planning/ROADMAP.md` § "Phase 04.1" — goal, 5 success criteria, constraints.

### Code that defines the current shape
- `src/domain/models.py` — `Product` (:38, docstring at :41-43 still claims "Represents a
  specific SKU including package size" — **that becomes false**), `ProductStore` (:68),
  the `uq_product_store` constraint (:96, → D-01), `PricePoint.unit_price_sek` (:119, → D-04).
- `src/domain/parser.py` :218-278 — the LLM extraction prompt and the
  `unit_price = price / pack_size` fallback (:264-266) that D-03 supersedes.
- `src/domain/result.py` — `PriceExtractionResult`, the dataclass D-08 extends.
- `src/api/templates/admin.html` :80-95, :525-545 — the v0.2.1 `PKG_UNITS` guided chain and
  its canonical-unit factors, which D-11 relocates to the link dialog.
- `alembic/versions/0001_initial.py` :82 — the constraint, and the file D-14 rewrites.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`PKG_UNITS` + the guided packaging chain** (`admin.html`, commit `6c2347f`): canonical
  units with ml→liter / g→kg factors and auto-label generation already exist and work. This
  phase **moves** it to the link dialog and **removes** its unit selector — it does not
  rebuild it.
- **`PricePoint.raw_data` (JSONB)** already captures the parser's raw response, so the
  scraped signals have a home even before promotion to columns.

### Established Patterns
- **Verbatim-port doctrine is OVER for this phase.** Phases 1–3 forbade model changes
  (byte-equivalent port). Phase 04.1 is explicitly a model change — CLAUDE.md's "don't
  refactor" constraint refers to the extraction, not to this.
- Tenant scoping: `DEFAULT_TENANT_ID` seeded constant; no `/me/context` lookups (Phase 3).

### Integration Points — the non-obvious ones
- **⚠ The notifier must now COMPUTE unit price.** `PriceWatch.unit_price_target_sek` and
  `unit_price_drop_threshold_percent` (`models.py:149-150`) compare unit prices, and the
  column they read (`PricePoint.unit_price_sek`) is being dropped by D-04. The notifier /
  watch-evaluation path must be rewired to `price_sek / link.package_quantity`, and must
  handle a NULL quantity (D-02) without crashing or firing a false alert.
- **`Product` docstring at `models.py:41-43`** encodes the old "product = SKU" model in
  prose. Update it, or it actively misleads the next reader.
- **`service.create_product()`** (`service.py:301-340`) takes `package_size` /
  `package_quantity` — signature changes; link creation (`link_product_store`, :350) gains
  them.
- **`admin.py` carries 28 `package_*` references** across create/update/detail/import paths
  (`:270`, `:310-324`, `:418`, `:475-480`, `:1325`, `:1500-1516`) — including a bulk-import
  path at :1500 that is easy to miss.
- **`schemas.py`** — `ProductCreate`/`Update` lose the package fields; link schemas gain them.
- **`tests/test_api.py`, `tests/test_service.py`** carry `package_*` references and will
  fail until updated.
- **`src/mcp_server/server.py`** :147 reads `unit_price_sek` from the row — affected by
  D-04/D-13 (the seed's "no direct package_* references found" is true but incomplete:
  the *unit-price* reference is the one that matters).

</code_context>

<specifics>
## Specific Ideas

- The canonical test case throughout: **Lambi toilet paper** — one product (unit = `st`,
  the comparison unit being "rulle"), several links across pack sizes and stores, the
  product page sorting them by kr/rulle. If that screen answers "which is cheapest per
  roll" at a glance, the phase succeeded.
- The Coop `kr/100g` row in D-05's table is the deliberate illustration of *why* the store's
  printed unit price cannot be the sortable column.

</specifics>

<deferred>
## Deferred Ideas

- **A middle `Variant` entity** (Product → Variant → per-store links) — the fully normalized
  model. Considered and **rejected** in the seed as over-modeling for a home tool. Not a
  future phase; a closed decision. Reopen only with a new reason.
- **Gray areas raised but not discussed** (surfaced during the wrap-up, user opted to
  proceed): whether `package_size` (the display label) is still needed at all now that
  amount + unit can generate it; how `find_deals` ranks now that unit price is computed;
  whether a product with zero links is valid. Planner may resolve these as they arise — none
  blocks the phase.

</deferred>

---

*Phase: 04.1-package-data-moves-to-the-store-link*
*Context gathered: 2026-07-14*
