---
phase: 04.1-package-data-moves-to-the-store-link
plan: 08
type: execute
wave: 7
depends_on: ["04.1-05", "04.1-06", "04.1-07"]
files_modified:
  - tests/conftest.py
  - tests/test_migration.py
  - pyproject.toml
  - README.md
autonomous: false
requirements: [MODEL-02, MODEL-08]

must_haves:
  truths:
    - "`alembic upgrade head` is green on a fresh Postgres and `alembic check` reports no drift (MODEL-08)"
    - "Two links at the same store PERSIST; a duplicate store_url is REJECTED — proven against a real DB, not a mock (MODEL-02)"
    - "kr/unit ORDER BY ranks the 24-pack above the 8-pack and sinks the NULL-quantity link to the bottom (MODEL-03)"
    - "The fast mocks-only suite still runs standalone with no database present"
    - "The D-15 deployed-instance reset is DOCUMENTED as an operator runbook — and was NOT executed by the agent"
  artifacts:
    - tests/conftest.py
    - tests/test_migration.py
    - "pytest `integration` marker"
    - "README.md § Schema reset (Phase 04.1)"
  key_links:
    - "aiosqlite CANNOT substitute for Postgres here — the models use postgresql.UUID and JSONB, which do not compile on SQLite. This is why no DB test has ever existed in this repo."
    - "A DB already stamped 0001_initial sees 0001_initial == head after the in-place rewrite, applies NOTHING, exits 0, and silently runs the OLD schema. Alembic does not checksum migration bodies. The reset is the only defence."
---

<objective>
Prove the schema — the thing this phase actually delivers — against a real database, and hand the operator a
copy-pasteable reset runbook.

Purpose: MODEL-02's "two links at one store persist" and "a duplicate store_url is rejected", and MODEL-08's
"upgrade head green on a fresh DB", are claims about POSTGRES. This repo has never had a DB-backed test: there
is no `conftest.py`, every test is an `AsyncMock`, and `aiosqlite` — though declared — cannot substitute,
because the models use `postgresql.UUID` and `JSONB`. Those requirements are currently provable only by a
human squinting at the app. The phase whose deliverable IS the schema is the phase that fixes that.

Output: a `conftest.py` hosting a real-Postgres engine that SKIPS cleanly when no database is reachable (so
the fast mocks-only suite still runs standalone in CI and on a laptop); `tests/test_migration.py` proving the
schema claims; a README runbook for the mandatory deployed-instance reset; and the human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-CONTEXT.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-RESEARCH.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-VALIDATION.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-PATTERNS.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-01-SUMMARY.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-06-SUMMARY.md
@.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-07-SUMMARY.md
</context>

## ⚠ Deployment boundary — read before running anything

**You may freely create, migrate, and DROP a LOCAL throwaway Postgres database** (a dedicated
`price_tracker_test` database, or a local container). That is verification.

**You must NOT run `docker compose down -v` against a deployed instance, and you must NOT deploy.** The D-15
reset acts on the live Postgres volume and is the OPERATOR's step, run when he is awake. This plan's job is to
WRITE that runbook, not to execute it. If any command you are about to run could touch the deployed stack,
stop and put it in the runbook instead.

## Artifacts this phase produces

This plan (**P08**):

| Artifact | Kind |
|---|---|
| `tests/conftest.py` — session-scoped async engine against a throwaway `price_tracker_test` DB, auto-skipping when Postgres is unreachable | new test infrastructure (the repo's first) |
| `tests/test_migration.py` — `upgrade head`, `alembic check`, two-links-same-store, duplicate-URL rejection, kr/unit ranking | new integration tests |
| pytest `integration` marker in `pyproject.toml` | config |
| `README.md` § "Schema reset (Phase 04.1)" | operator runbook |

<tasks>

<task type="auto">
  <name>Task 1: Create the DB-backed test tier — conftest.py with an auto-skipping Postgres fixture</name>
  <files>tests/conftest.py, pyproject.toml</files>

  <read_first>
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-PATTERNS.md` § "No Analog Found" — there is no `conftest.py` in this repo and nothing to copy. It lists the three things that ARE the closest fixture patterns: `docker-compose.yml` lines 9-25 (the local Postgres, credentials in plain sight, published on 5432), `src/infra/db.py` lines 16-22 (the engine + `DATABASE_URL` default a fixture should reuse or override), and `alembic/env.py` lines 28-29 and 75-90 (the only async-engine construction in the repo, and the `DATABASE_URL`-override convention).
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-RESEARCH.md` § "Common Pitfalls" Pitfall 6 — why `aiosqlite` cannot be used here, and the option (b) this plan implements.
    - `pyproject.toml` § `[tool.pytest.ini_options]` — `asyncio_mode = "auto"`, `testpaths = ["tests"]`.
  </read_first>

  <action>
Create `tests/conftest.py` — the repo's first. Its ONE job is to make a real Postgres available to the
integration tier without breaking the fast mocks-only suite that has always run with no database.

- Resolve the test database URL from `TEST_DATABASE_URL`, falling back to `DATABASE_URL`, falling back to the
  local-dev default in `src/infra/db.py`. Override the DATABASE NAME to a dedicated throwaway
  (`price_tracker_test`) so a test run can never touch the developer's dev database — this test tier
  CREATE-DROPs its schema, and pointing it at the working DB by accident is the kind of thing that only
  happens once.
- A session-scoped fixture that connects to the server's maintenance database, drops `price_tracker_test` if
  present, and recreates it — a genuinely fresh DB, which is what MODEL-08 claims ("`upgrade head` migrates a
  FRESH DB cleanly").
- **The skip guard is the load-bearing part.** If no Postgres is reachable, the fixture must call
  `pytest.skip` with a message naming the URL it tried and how to start one (`docker compose up -d postgres`).
  It must NOT fail, and it must NOT hang: give the probe connection an explicit short timeout. A developer or a
  CI job with no database must still get a green `poetry run pytest` from the mocks-only suite — that is the
  contract that lets this tier exist at all.
- A fixture that runs Alembic programmatically against the fresh DB (`alembic.config.Config` pointed at
  `alembic.ini`, with the sqlalchemy URL overridden to the test URL, then `command.upgrade(cfg, "head")`),
  following `alembic/env.py`'s existing async-engine convention.
- A session factory fixture yielding an `AsyncSession` against the migrated test DB, with each test rolled
  back or the tables truncated between tests so ordering cannot matter.

Register the `integration` marker in `pyproject.toml` under `[tool.pytest.ini_options]` so
`@pytest.mark.integration` does not emit a warning, and so the tier is selectable
(`pytest -m integration` / `pytest -m "not integration"`). Add no new dependency: `asyncpg` and `alembic` are
already present, and this is deliberate — the temptation is to reach for `pytest-postgresql` or
`testcontainers`, and neither is needed when the repo already runs Postgres in `docker-compose.yml`.

Do not modify any existing test file. The mocks-only suite must keep passing untouched, and its runtime must
stay under a few seconds.
  </action>

  <verify>
    <automated>cd /home/magnus/dev/price-tracker && poetry run pytest -m "not integration" -q && poetry run python -c "import pathlib; s=pathlib.Path('pyproject.toml').read_text(); assert 'integration' in s and 'markers' in s; print('marker registered')"</automated>
  </verify>

  <acceptance_criteria>
    - `poetry run pytest -m "not integration" -q` exits 0 — the mocks-only suite is unaffected and still needs no database.
    - With Postgres STOPPED, `poetry run pytest -q` exits 0 with the integration tests reported as SKIPPED (not failed, not errored, and not hanging). Verify this explicitly by running the suite with the database down.
    - With Postgres RUNNING, the session fixture creates a fresh `price_tracker_test` database and the integration tests execute against it.
    - No test run touches the `price_tracker` (dev) database — the fixture overrides the database name.
    - `pyproject.toml` registers the `integration` marker and declares no new dependency.
  </acceptance_criteria>

  <done>The repo has a DB-backed test tier that runs when Postgres is there and skips cleanly when it is not; the fast suite is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the schema against a real Postgres — tests/test_migration.py</name>
  <files>tests/test_migration.py</files>

  <read_first>
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-VALIDATION.md` § "Per-Task Verification Map" — the MODEL-02, MODEL-03, and MODEL-08 rows are the ones this file discharges. They are the requirements that no mock can prove.
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-RESEARCH.md` § "Alembic In-Place Rewrite" (the `alembic check` gate and the three drift traps) and § "Code Examples" (the kr/unit ranking query).
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-01-SUMMARY.md` — the final column and constraint names.
    - `tests/conftest.py` (Task 1) — the fixtures.
  </read_first>

  <action>
Create `tests/test_migration.py`, every test marked `@pytest.mark.integration`. Each one discharges a claim
that the mocks-only suite structurally cannot:

- **`test_upgrade_head_creates_reshaped_schema` (MODEL-08).** After `upgrade head` on the fresh DB, inspect
  the live schema (SQLAlchemy `inspect`): `product_stores` has `package_size`, `package_quantity`, and
  `scraped_package_quantity`; `products` has NO column whose name starts with `package_`; `price_points` has
  `store_unit_price_sek` and does NOT have `unit_price_sek`; the unique constraint
  `uq_product_stores_store_url` exists and `uq_product_store` does not; and the five stores are seeded.
- **`test_alembic_check_reports_no_drift` (MODEL-08).** Run Alembic's `check` command against the migrated DB
  and assert it reports no new operations. This is the cheapest possible proof that the rewritten migration
  and the rewritten `models.py` actually agree — and this project has already been bitten once by exactly this
  drift (STATE.md, Plan 01-03), so it is not a hypothetical.
- **`test_two_links_same_store_persist` (MODEL-02, must match `-k two_links_same_store`).** Insert a Lambi
  product (unit `st`) and two `ProductStore` rows pointing at the SAME seeded Willys store with DIFFERENT URLs
  and different pack sizes (24 and 8). Both must commit. **This is the phase's whole reason to exist**, and
  before D-01 it was a constraint violation.
- **`test_duplicate_store_url_rejected` (MODEL-02, must match `-k duplicate_url`).** A second link with a URL
  that is already tracked must raise `IntegrityError` on flush. Same page, tracked twice — blocked.
- **`test_unit_price_order_by_ranks_cheapest_per_unit` (MODEL-03, must match `-k order_by`).** Three links with
  latest price points — a 24-pack at 139.90 (5.83/st), an 8-pack at 59.90 (7.49/st), and a link with a NULL
  `package_quantity` — ordered by `unit_price_expr(...)` ascending with `.nulls_last()`. Assert the 24-pack
  comes first, the 8-pack second, and the NULL-quantity link LAST. This is AC#3's ranking proven in SQL rather
  than asserted in a mock, and it is the one place the hybrid's SQL side meets a real query planner.
- **`test_offer_price_wins_the_computed_unit_price` (MODEL-03).** A link whose latest price point carries an
  offer price ranks by the OFFER's unit price, not the regular one.

Use the seeded stores from the migration rather than inserting your own — that also proves the seed survived
the rewrite.
  </action>

  <verify>
    <automated>cd /home/magnus/dev/price-tracker && docker compose up -d postgres && poetry run pytest tests/test_migration.py -m integration -q && poetry run pytest -q</automated>
  </verify>

  <acceptance_criteria>
    - `poetry run pytest tests/test_migration.py -m integration -q` exits 0 against a real Postgres, with all six tests passing.
    - `poetry run pytest tests/test_migration.py -k two_links_same_store -x` passes — two links at one store persist (MODEL-02).
    - `poetry run pytest tests/test_migration.py -k duplicate_url -x` passes — a repeated `store_url` raises `IntegrityError` (MODEL-02).
    - `poetry run pytest tests/test_migration.py -k order_by -x` passes — 24-pack, then 8-pack, then the NULL-quantity link last (MODEL-03).
    - `test_alembic_check_reports_no_drift` passes — the migration DDL and the ORM metadata agree, by constraint NAME (MODEL-08).
    - `poetry run pytest -q` (full suite, integration included, Postgres up) exits 0.
    - The only database created or dropped during this task is the throwaway `price_tracker_test`. No deployed instance was touched.
  </acceptance_criteria>

  <done>The schema claims are proven against Postgres: the migration is green on a fresh DB, drifts nowhere, permits two links at one store, rejects a duplicate URL, and ranks links correctly by computed kr/unit.</done>
</task>

<task type="auto">
  <name>Task 3: Write the operator's reset runbook (do NOT execute it)</name>
  <files>README.md</files>

  <read_first>
    - `README.md` — the existing runbook, env-var contract, security model, and release-flow sections (added 2026-07-13, commit a295e2f). Match its voice and structure; add a section, do not restructure the file.
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-CONTEXT.md` § D-15 — the mandatory reset, verbatim.
    - `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-RESEARCH.md` § "Alembic In-Place Rewrite" — the precise mechanism of the silent failure.
    - `.planning/STATE.md` § Decisions D-27, D-30 — how this app is actually deployed (home-server repo, Dokploy, pinned GHCR tag, `alembic upgrade head` in the compose command). The runbook must describe THAT deployment, not the local `docker-compose.yml`, which is local-dev-only.
  </read_first>

  <action>
Add a section to `README.md`: **"Schema reset (Phase 04.1) — required once, by the operator"**.

State the failure mode first, because it is silent and therefore easy to skip:
`alembic/versions/0001_initial.py` was rewritten IN PLACE (D-14 — the DB was empty, so a clean single
migration beat stacking a 0002). The `alembic_version` table holds only a revision id; Alembic does NOT
checksum migration bodies. A database already stamped `0001_initial` therefore compares equal to head, applies
NOTHING, and exits 0 with no output — after which the app runs against the OLD schema and fails at the first
query touching `product_stores.package_quantity`. Nothing warns you. The reset is the only defence.

Then give the copy-pasteable steps, targeting the DEPLOYED stack (the home-server / Dokploy compose for
price-tracker — not this repo's local-dev `docker-compose.yml`): stop the app, drop the Postgres volume so
both the tables AND `alembic_version` go away, bring Postgres back up, run `alembic upgrade head` (which now
runs the rewritten 0001 against an empty database), and then verify with `alembic current` (must report
`0001_initial` as head) and `alembic check` (must report no new upgrade operations). Note that the deploy
compose already runs `alembic upgrade head` in its command, so the operative step is the volume drop.

State plainly that this destroys all price history — and that this is accepted, because the DB is empty and
the phase was sanctioned on exactly that basis.

Also note the export-format break flagged in 04.1-06: an export file produced before this phase will not
round-trip through the new import (package data moved under the link; the price rows carry a renamed column).
If the operator is holding an export he intends to restore, he must say so BEFORE running the reset.

**Do not execute any of this.** Do not run `docker compose down -v`. Do not deploy. The runbook is the
deliverable; the operator runs it when he is awake.
  </action>

  <verify>
    <automated>cd /home/magnus/dev/price-tracker && grep -q "Schema reset" README.md && grep -q "alembic check" README.md && grep -q "alembic current" README.md && echo "runbook present" && git log --oneline -1 -- README.md</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` contains a "Schema reset (Phase 04.1)" section naming the silent-no-op failure mode, the volume-drop requirement, and the two verification commands (`alembic current`, `alembic check`).
    - The runbook targets the DEPLOYED stack (home-server / Dokploy compose), not this repo's local-dev-only `docker-compose.yml`.
    - The runbook states that the reset destroys price history and that this is accepted because the DB is empty.
    - The runbook flags the export-format break.
    - **No reset command was executed against any deployed instance during this task.** The only database touched anywhere in this plan is the local throwaway `price_tracker_test`.
  </acceptance_criteria>

  <done>The operator has a copy-pasteable reset runbook that explains why skipping it fails silently — and the agent has not run it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CHECKPOINT — operator verifies the live scrape (AC#4), the product page (AC#3), and owns the D-15 reset</name>

  <action>
STOP. Do not proceed past this task, and do not close the phase. Present the verification script below to the
operator and wait for his response.

Two of the phase's five acceptance criteria cannot be self-certified: AC#4 needs a live network call and a
real LLM extraction against a real store page, and AC#3 is a visual/UX judgement about whether one screen
answers the toilet-paper question. A third item — the D-15 reset — acts on the live Postgres volume and is the
operator's step by design (see the deployment boundary at the top of this plan).

Do NOT run the reset. Do NOT deploy. Do NOT mark AC#3 or AC#4 as passing on the strength of the test suite.
  </action>

  <what-built>
Phase 04.1 is code-complete and the full suite (mocks + Postgres integration) is green:
- `package_size` + `package_quantity` moved `Product` → `ProductStore`; `unit` stays on `Product`.
- `uq_product_store` dropped; `store_url` globally unique; the three endpoints that would have 500'd are
  re-keyed to `/product-stores/{id}`.
- Unit price computed on read (`price / link.package_quantity`), never stored. `PricePoint.unit_price_sek`
  dropped; `store_unit_price_sek` holds the store's printed value as a raw, never-sorted signal.
- The scraper fills or flags the link's quantity in all three write-paths and never overwrites it.
- Product page lists links with a sortable kr/unit column; MCP `compare_stores` is one row per link.
- `alembic upgrade head` is green on a fresh DB and `alembic check` reports no drift.

Two things need a human, and neither can be self-certified:
  </what-built>

  <how-to-verify>
**1. AC#4 — the scraper against a real store page** (needs live network + a real LLM call):
   a. Start the app locally against the local Postgres.
   b. Create a product: "Lambi" — unit `st`. Confirm the dialog asks for NOTHING about packaging (AC#1).
   c. Link it to Willys with a real toilet-paper product URL, and LEAVE THE AMOUNT BLANK.
   d. Open the product's Links panel. The link should show "saknar mängd" in the kr/unit column.
   e. Hit "Check now". Expect: a price appears AND `package_quantity` is autofilled from the page.
   f. Now edit the amount to a deliberately WRONG value (say 12 when the page says 24) and hit "Check now"
      again. Expect: the link is FLAGGED as a mismatch and your value is NOT overwritten. That is D-07 —
      the typed value is intent, the page is evidence, and evidence does not get to rewrite intent.

**2. AC#3 — the toilet-paper question** (visual/UX judgement):
   Link a second pack size at the SAME store (a Willys 8-pack alongside the 24-pack) — this is the exact
   scenario that returned HTTP 500 before this phase — and a third at another store. Open the Links panel.
   Expect: all three links listed, sorted cheapest-per-roll first, with the store's own printed comparison
   price in a separate muted column. Ask yourself the actual question: **can you tell which pack is cheapest
   per roll at a glance?** If not, the phase failed, whatever the tests say.

**3. Two decisions to confirm** (both were the planner's call; either can be reversed cheaply):
   - `find_deals` still ranks by RECENCY; kr/unit is exposed as data but is not the sort key.
   - MCP `compare_stores` has been printing "N/A" in its comparison-price column and "Nej" for stock on
     EVERY row since extraction — a dead-key bug, fixed incidentally here. The `priser` skill has never
     actually seen a comparison price.

**4. Then, and only then — the D-15 reset.** The runbook is in `README.md` § "Schema reset (Phase 04.1)".
   **The agent has deliberately NOT run it.** The deployed DB is stamped at the old `0001_initial`, so
   `alembic upgrade head` will silently apply nothing and the app will run the OLD schema until the volume is
   dropped. Run it yourself when you are ready to deploy.
  </how-to-verify>

  <resume-signal>Type "approved" to close the phase, or describe what the Links panel failed to answer.</resume-signal>

  <verify>
    <human-check>Operator confirms AC#3 (the Links panel answers "which pack is cheapest per roll" at a glance) and AC#4 (a real "Check now" autofills an empty quantity, and a second one FLAGS a wrong quantity without overwriting it).</human-check>
  </verify>

  <done>Operator has replied "approved" (or filed the gaps), and has acknowledged that the D-15 reset is his to run. The phase does not close on the test suite alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → developer's database | A misconfigured fixture URL could CREATE-DROP the working dev database |
| rewritten migration → deployed database | A stamped DB silently no-ops the rewritten revision and runs the old schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-24 | Denial of Service (data loss) | `conftest.py` DB fixture | high | mitigate | The fixture overrides the database NAME to a dedicated throwaway (`price_tracker_test`) rather than reusing `DATABASE_URL` as-is, so a test run can never CREATE-DROP the dev or deployed database |
| T-04.1-25 | Denial of Service (silent) | in-place migration rewrite | **critical** | mitigate | Documented reset runbook + `alembic current` / `alembic check` verification steps. Alembic does not checksum bodies, so nothing detects the edit; a stamped DB runs the OLD schema with no error until the first query touches a moved column. The runbook is the only control, which is why it is a deliverable and not a footnote. |
| T-04.1-26 | Denial of Service | agent executing the reset | high | mitigate | The deployment boundary is stated at the top of this plan and repeated in Task 3's acceptance criteria: the agent writes the runbook, the operator runs it. The plan is `autonomous: false` and gated on a blocking human checkpoint. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase — `asyncpg` and `alembic` were already present; `pytest-postgresql` and `testcontainers` were deliberately NOT added |
</threat_model>

<verification>
Phase gate — all of it must be green:

1. `poetry run pytest -m "not integration" -q` — the fast suite, no database needed.
2. `docker compose up -d postgres && poetry run pytest -q` — the full suite including the Postgres tier.
3. `alembic upgrade head` against the fresh throwaway DB, then `alembic current` (reports `0001_initial`) and
   `alembic check` (reports no new upgrade operations).
4. `poetry run ruff check src tests` is clean.
5. `tests/test_static_gates.py` is green — no pair-keyed link lookup survives anywhere in `src/`, and the
   Python and JS package-factor tables agree.
6. With Postgres STOPPED, `poetry run pytest -q` still exits 0 (integration tests SKIP, they do not fail).

**Not verified by the agent, by design:** the D-15 reset against the deployed instance, and AC#4's live
store-page scrape. Both are on the operator, at the checkpoint.
</verification>

<success_criteria>
- MODEL-08: `alembic upgrade head` green on a fresh Postgres; `alembic check` reports no drift; the full test suite passes.
- MODEL-02: two links at one store persist and a duplicate `store_url` is rejected — proven against a real database.
- MODEL-03: the kr/unit ranking is proven in SQL, with NULL-quantity links sorting last.
- The mocks-only suite still runs standalone with no database.
- The operator holds a runbook for the one step the agent must not take.
</success_criteria>

<output>
Create `.planning/phases/04.1-package-data-moves-to-the-store-link/04.1-08-SUMMARY.md` when done.
Include the full reset runbook verbatim in the SUMMARY (not just a pointer to README.md) — it is the phase's
handoff to the operator, and it must survive even if he never opens the README.
</output>
