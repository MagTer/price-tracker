---
phase: 04-mcp-server-agent-wiring
plan: 01
subsystem: mcp
tags: [fastmcp, mcp, bearer-auth, retroactive]

requires:
  - phase: 03-admin-ui-iap-header-trust
    provides: FastAPI app factory shared by the MCP mount
provides:
  - src/mcp_server/server.py (4 MCP tools)
  - src/mcp_server/auth.py (BearerTokenMiddleware)
affects: []

tech-stack:
  added: [fastmcp 2.x]
  patterns: [bearer-token ASGI middleware wrapping a mounted sub-app, sub-app lifespan propagation into the parent FastAPI lifespan]

key-files:
  created:
    - src/mcp_server/__init__.py
    - src/mcp_server/auth.py
    - src/mcp_server/server.py
  modified:
    - src/api/app.py
    - pyproject.toml

key-decisions:
  - "Retroactive plan: MCP server scaffolded in commit d92372a (2026-05-04), substantially fixed today (2026-07-06: package rename, fastmcp 1.0-to-2.x bump, lifespan propagation - quick tasks 260706-rso/260706-t3p). This PLAN.md written 2026-07-06 for GSD tracking."
  - "This phase's goal is only PARTIALLY complete — see 04-CONTEXT.md and 04-VERIFICATION.md. Criterion 1 (server itself) is done; criteria 2-4 (subdomain ingress, agent-platform registration, live query) are not, and are NOT part of what this plan's task covers."
---

<objective>
Retroactively document the part of Phase 4 that was actually built: a FastMCP server exposing 4 price-tracker tools, mounted on the FastAPI app, protected by static bearer-token auth.

Explicitly does NOT cover (and this plan makes no claim about): the mcp.<domain> subdomain/ingress, agent-platform registration, or a live end-to-end agent query — those success criteria are unmet; see 04-VERIFICATION.md for the gaps_found status.
</objective>

<tasks>

<task type="auto">
  <name>Task 1 (retroactive): FastMCP server + bearer auth, mounted on FastAPI</name>
  <files>src/mcp_server/__init__.py, src/mcp_server/auth.py, src/mcp_server/server.py, src/api/app.py</files>
  <action>
Already implemented (commit d92372a scaffold + today's fixes, commits 04a1479/936f9ca/146aafe/7a3127b). No new action — this task records what was built:
- src/mcp_server/server.py: FastMCP("Price Tracker") with 4 @mcp.tool() functions (check_price, find_deals, compare_stores, list_products); get_mcp_app() returns (wrapped_app, http_app) using fastmcp 2.x's http_app()
- src/mcp_server/auth.py: BearerTokenMiddleware validating Authorization: Bearer MCP_BEARER_TOKEN
- src/api/app.py: mounts the wrapped app at /mcp; lifespan enters the raw http_app's own lifespan so the streamable-HTTP session manager starts/stops correctly
  </action>
  <verify>
    <automated>test -f src/mcp_server/server.py && test -f src/mcp_server/auth.py && grep -q "app.mount" src/api/app.py && poetry run pytest -q</automated>
  </verify>
  <done>MCP server files exist with the described responsibilities; mounted correctly; full test suite green. Criteria 2-4 (subdomain, agent registration, live query) remain explicitly unmet — not in scope of this task.</done>
</task>

</tasks>

<output>
Retroactive plan — SUMMARY.md and VERIFICATION.md for this phase document actual verification evidence gathered 2026-07-06, including the unmet criteria.
</output>
