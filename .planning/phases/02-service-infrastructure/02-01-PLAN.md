---
phase: 02-service-infrastructure
plan: 01
subsystem: infra
tags: [httpx, aiosmtplib, openrouter, fastapi, lifespan, retroactive]

requires:
  - phase: 01-skeleton-domain-copy
    provides: domain modules, squashed migration, green pytest suite, Dockerfile/compose skeleton
provides:
  - src/infra/fetcher.py (WebFetcher - httpx async client + HTML text extraction)
  - src/infra/email.py (SmtpEmailService - aiosmtplib)
  - src/infra/llm.py (OPENROUTER_BASE_URL/HEADERS)
  - src/infra/providers.py (singleton get_fetcher/get_email_service)
  - src/api/app.py (create_app() factory + lifespan starting PriceCheckScheduler)
affects: [03-admin-ui-iap-header-trust, 04-mcp-server-agent-wiring]

tech-stack:
  added: [httpx (already a dep), aiosmtplib, uvicorn]
  patterns: [singleton-per-process provider functions, lifespan-scoped scheduler startup]

key-files:
  created:
    - src/infra/fetcher.py
    - src/infra/email.py
    - src/infra/llm.py
    - src/infra/providers.py
    - src/api/app.py
    - src/api/__init__.py
  modified:
    - src/infra/db.py
    - pyproject.toml
    - Dockerfile
    - docker-compose.yml
    - .env.template

key-decisions:
  - "Retroactive plan: this phase was implemented directly in commit d92372a (2026-05-04) rather than through /gsd-plan-phase. This PLAN.md was written on 2026-07-06 to reconcile GSD phase-tracking with the actual delivered code — see 02-CONTEXT.md for full rationale."
---

<objective>
Retroactively document Phase 2's implementation: replace the source repo's `IFetcher`/`IEmailService`/LiteLLM-proxy/in-memory-session interfaces with concrete infra clients (httpx, aiosmtplib, OpenRouter direct), and wire them into a FastAPI app whose lifespan starts the price-check scheduler.

This plan describes work already completed in commit `d92372a` (2026-05-04) — it is written after the fact to give this phase the formal PLAN.md artifact GSD's tracking requires, not to direct new work.
</objective>

<tasks>

<task type="auto">
  <name>Task 1 (retroactive): Infra clients + FastAPI app factory</name>
  <files>src/infra/fetcher.py, src/infra/email.py, src/infra/llm.py, src/infra/providers.py, src/infra/db.py, src/api/app.py, src/api/__init__.py</files>
  <action>
Already implemented in commit d92372a. No new action — this task records what was built:
- src/infra/fetcher.py: WebFetcher class wrapping httpx.AsyncClient with browser headers, 15s/5s timeouts, stdlib HTMLParser-based text extraction (strips script/style/nav/footer/header, truncates at 12000 chars)
- src/infra/email.py: SmtpEmailService wrapping aiosmtplib, is_configured() gate
- src/infra/llm.py: OPENROUTER_BASE_URL/API_KEY/HTTP_REFERER/APP_TITLE env vars + OPENROUTER_HEADERS dict
- src/infra/providers.py: get_fetcher()/get_email_service() singleton accessors
- src/infra/db.py: Base declarative class, _utc_now(), async_session_factory
- src/api/app.py: create_app() FastAPI factory; lifespan starts PriceCheckScheduler with the shared fetcher/email singletons; exposes app.state.scheduler
  </action>
  <verify>
    <automated>test -f src/infra/fetcher.py && test -f src/infra/email.py && test -f src/infra/llm.py && test -f src/infra/providers.py && test -f src/api/app.py && poetry run pytest -q</automated>
  </verify>
  <done>All 6 files exist with the described responsibilities; full test suite green.</done>
</task>

</tasks>

<output>
Retroactive plan — SUMMARY.md and VERIFICATION.md for this phase describe actual verification evidence gathered 2026-07-06.
</output>
