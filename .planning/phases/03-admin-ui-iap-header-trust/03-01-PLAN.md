---
phase: 03-admin-ui-iap-header-trust
plan: 01
subsystem: api
tags: [fastapi, admin-ui, iap, header-trust, retroactive]

requires:
  - phase: 02-service-infrastructure
    provides: FastAPI app factory, infra clients
provides:
  - src/api/admin.py (14+ admin endpoints, IAP-gated)
  - src/api/auth.py (require_auth dependency)
  - src/api/schemas.py (Pydantic request/response models)
  - src/api/templates/admin.html (dashboard UI)
affects: [04-mcp-server-agent-wiring]

tech-stack:
  added: []
  patterns: [IAP header-trust auth dependency, router-level Depends() auth gate]

key-files:
  created:
    - src/api/admin.py
    - src/api/auth.py
    - src/api/schemas.py
  modified:
    - src/api/templates/admin.html
    - docker-compose.yml

key-decisions:
  - "Retroactive plan: implemented directly in commit d92372a (2026-05-04); admin.html rebuilt further and committed 2026-07-06 (16245a4). This PLAN.md written 2026-07-06 to reconcile GSD tracking with delivered code — see 03-CONTEXT.md."
---

<objective>
Retroactively document Phase 3's implementation: port the admin UI (endpoints, schemas, template) behind IAP header-trust auth, replacing Entra OIDC code-flow and context_id-based tenant lookups.

Describes work already completed in commit d92372a (2026-05-04) plus the admin.html rebuild committed 2026-07-06 (16245a4) — written after the fact for GSD tracking, not to direct new work.
</objective>

<tasks>

<task type="auto">
  <name>Task 1 (retroactive): Admin endpoints + IAP auth + dashboard template</name>
  <files>src/api/admin.py, src/api/auth.py, src/api/schemas.py, src/api/templates/admin.html</files>
  <action>
Already implemented across commits d92372a and 16245a4. No new action — this task records what was built:
- src/api/auth.py: require_auth() dependency, X-Auth-Request-Email header trust, ALLOWED_ENTRA_EMAIL validation, 403 on mismatch
- src/api/admin.py: 14+ endpoints (stores, products, product-stores, price history, deals, watches) all behind require_auth, using DEFAULT_TENANT_ID
- src/api/schemas.py: Pydantic request/response models
- src/api/templates/admin.html: full dashboard (sidebar, product/watch management, Chart.js price history)
  </action>
  <verify>
    <automated>test -f src/api/admin.py && test -f src/api/auth.py && test -f src/api/schemas.py && grep -q "require_auth" src/api/admin.py && poetry run pytest -q</automated>
  </verify>
  <done>All 4 files exist with the described responsibilities; full test suite green.</done>
</task>

</tasks>

<output>
Retroactive plan — SUMMARY.md and VERIFICATION.md for this phase describe actual verification evidence gathered 2026-07-06.
</output>
